<nav class="wmnds-grid justify-between">
  {% for navItem in navItems %}
    <div class="wmnds-col-1 wmnds-col-md-1-4">
      <a href="/{{ navItem.name | lower | replace(" ", "-") }}/" title="{{ navItem.name }} page" target="_self" class="wmnds-btn  wmnds-btn__primary  mar-bot-md" >
        {{ navItem.name}}
      </a>
    </div>
  {% endfor %}
</nav>