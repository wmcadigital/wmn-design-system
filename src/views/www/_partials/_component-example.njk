{% macro compExample(componentPath, classNames) %}

<div class="wmnds-col-1 {{classNames}}">
  {%- for item in componentPath %}
    {{ item }}
  {% endfor -%}
</div>

<h4>HTML Markup</h4>
{# Be careful with the below, the whitespace is extemely delicate (like a flower 🌸) #}
<pre><code class="html">
{%- for item in componentPath -%}{{ item | forceescape | trim }}
{% endfor -%}
</code></pre>
<hr>
{% endmacro %}
