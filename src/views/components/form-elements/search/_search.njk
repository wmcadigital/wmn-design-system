{% macro wmndsSearch(params) %}

{% from "components/icon/_icon.njk" import wmndsIcon %}
{% from "components/disruption-indicator/medium/_medium.njk" import wmndsDisruptionIndicatorMedium %}


<div class="wmnds-fe-autocomplete wmnds-grid">
  {# Get SVG icon and pass class #}
  {{-
    wmndsIcon({
      icon: 'general-search',
      class: 'wmnds-fe-autocomplete__icon'
    }) | indent(6) | trim
  }}
  <input type="text" name="searchExample" aria-label="search for..." class="wmnds-fe-autocomplete__input wmnds-col-1" data-placeholder="Tap to search" data-api="https://jsonplaceholder.typicode.com/users" data-keys="name, email" value="x15" />
</div>

<ul class="wmnds-fe-autocomplete-results">
  {%- for i in range(1,4) %}
  <li class="wmnds-grid" title="Good service on X15">
    <div class="bus_indicator__1zdUw wmnds-col-auto">
      {{
        wmndsDisruptionIndicatorMedium({
          classModifiers: 'wmnds-disruption-indicator-medium'
        }) | indent(6) | trim
      }}
    </div>
    <div class="wmnds-col-1-2 wmnds-col-sm-2-3">
      <strong>Oldbury to Dudley {{i}}</strong><br>and return journey
    </div>
  </li>
  {%- endfor %}
</ul>




{% endmacro %}