{% macro wmndsLabel(params) %}
{# Set vars and defaults #}
{% set for = params.for if params.for else "genericInput" %}
{% set _label = params.contentText if params.contentText else "Form label" %}

<label class="wmnds-fe-label" for="{{for}}">{{ _label }}</label>

{% endmacro %}