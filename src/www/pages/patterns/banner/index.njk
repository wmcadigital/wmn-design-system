{% extends "www/_layouts/layout-left-pane.njk" %}
{% set pageTitle = "Banner" %}
{% set section="Patterns" %}
{% from "www/_partials/component-example/component-example.njk" import compExample %}
{% from "wmnds/patterns/banner/_banner.njk" import wmndsBanner %}

{% block content %}

{# About #}
<h2>About</h2>
{# What #}
<h3>What does it do?</h3>
<ul>
  <li>Helps users to identify when a service is new</li>
  <li>Offers users the opportunity to give feedback about a new service</li>
</ul>
{# When to #}
<h3>When to use it?</h3>
<ul>
  <li>When a service is new</li>
</ul>
{# When not to #}
<h3>When not to use it?</h3>
<ul>
  <li>On a service that has matured and has a steadily positive rate of satisfaction</li>
</ul>
{# How #}
<h3>How it works</h3>
<ul>
  <li>A service can be in Alpha (prototype stage) or Beta (when it is on a publicly-accessible website)</li>
  <li>The survey link must point to a Hotjar or service feedback survey, with the service name within the URL. For example, <code class="wmnds-website-inline-code">/?serviceName=descriptiveName</code>.</li>
</ul>
<p>A service can either be in Alpha (prototype stage) or Beta (when it is on a publicly accessible website).</p>
<hr>
<br><br>

{{
  compExample([
   wmndsBanner()
  ])
}}

{% endblock %}
